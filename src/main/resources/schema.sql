CREATE TABLE IF NOT EXISTS restriccion (
    ultimo_digito INT NOT NULL,
    dia_semana VARCHAR(50) NOT NULL,
    start_time_morning TIME NOT NULL,
    end_time_morning TIME NOT NULL,
    start_time_afternoon TIME NOT NULL,
    end_time_afternoon TIME NOT NULL
);



